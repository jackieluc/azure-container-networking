parameters:
  name: ""
  clusterName: ""
  scaleup: ""

steps:
  - bash: |
      echo $UID
      sudo rm -rf $(System.DefaultWorkingDirectory)/*
    displayName: "Set up OS environment"

  - checkout: self

  - bash: |
      go version
      go env
      mkdir -p '$(GOBIN)'
      mkdir -p '$(GOPATH)/pkg'
      mkdir -p '$(modulePath)'
      echo '##vso[task.prependpath]$(GOBIN)'
      echo '##vso[task.prependpath]$(GOROOT)/bin'
    name: "GoEnv"
    displayName: "Set up the Go environment"

  - task: KubectlInstaller@0
    inputs:
      kubectlVersion: latest

  - task: AzureCLI@2
    displayName: Set cluster credentials
    inputs:
      azureSubscription: $(BUILD_VALIDATIONS_SERVICE_CONNECTION)
      scriptLocation: inlineScript
      scriptType: bash
      inlineScript: make -C ./hack/aks set-kubeconf AZCLI=az CLUSTER=${{ parameters.clusterName }}

  - template: ../../templates/cilium-cli.yaml

  - script: |
      echo "Start Azilium E2E Tests"
      kubectl get po -owide -A
      # These vars may be set by the job template via setup stage outputs and are available here as environment variables in child scripts.

      IPAM=${AZURE_IPAM_VERSION:-$(make azure-ipam-version)}
      CNS=${CNS_VERSION:-$(make cns-version)}
      sudo -E env "PATH=$PATH" make test-load SCALE_UP=32 OS_TYPE=linux VALIDATE_STATEFILE=true INSTALL_AZILIUM=true AZURE_IPAM_VERSION=${IPAM} CNS_VERSION=${CNS} CLEANUP=true
    retryCountOnTaskFailure: 3
    name: "aziliumTest"
    displayName: "Run Azilium E2E"

  - template: ../../templates/cilium-tests.yaml
    parameters:
      clusterName: ${{ parameters.clusterName }}
      scaleup: ${{ parameters.scaleup }}
